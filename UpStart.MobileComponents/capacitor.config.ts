import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.upstart.mobilecomponents',
  appName: 'UpStart Ionic',
  webDir: 'www'
};

export default config;
